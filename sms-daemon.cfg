# sms-daemon configuration example.
# If a key is omitted, the compiled default is used.
device=/dev/ttyUSB2
job_dir=/tmp/outsms
sms_dir=/tmp/sms-daemon/ReceivedSMS
ussd_dir=/tmp/sms-daemon/ReceivedSMS
log_file=/tmp/sms-daemon/Log
debug=true
